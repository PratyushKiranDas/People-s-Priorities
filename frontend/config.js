// Edit these two values for your deployment.

// URL of your running FastAPI backend (see backend/README or Cloud Run service URL)
window.PEOPLES_PRIORITIES_API_BASE = "http://localhost:8080";

// Optional: a Google Maps JavaScript API key (https://console.cloud.google.com/google/maps-apis)
// Leave blank to skip the map — the ranked priority list still works without it.
window.PEOPLES_PRIORITIES_MAPS_KEY = "";
